var countSegments = function (s) {
    let count = 0
    let newArr = s.split(" ")
    for (let i in newArr) {
        if (newArr[i] !== " " && newArr[i] !== "") {
            count++
        }
    }
    return count
};
